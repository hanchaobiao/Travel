<template>
  <div>
    <div class="banner" @click="handleBannerClick">
      <img class="banner-img" :src="bannerImg">
      <div class="banner-info">
        <div class="banner-title">{{sightName}}</div>
        <div class="banner-number"><span class="iconfont banner-icon">&#xe691;</span>&nbsp;{{gallaryImgs.length}}</div>
      </div>
    </div>
    <common-gallary :images="gallaryImgs" v-show="showGallary" @close="handleGallartClose"></common-gallary>
  </div>
</template>

<script>
import CommonGallary from 'common/gallary/Gallary'

export default {
  name: 'DetailBanner',
  props: ['bannerImg', 'sightName', 'gallaryImgs'],
  components: {
    CommonGallary
  },
  data () {
    return {
      showGallary: ''
    }
  },
  methods: {
    handleBannerClick () {
      this.showGallary = true
    },
    handleGallartClose () {
      this.showGallary = false
    }
  }
}
</script>

<style lang="stylus" scoped>
  .banner
    position: relative
    overflow: hidden
    height: 0
    padding-bottom: 55%
    .banner-img
      width: 100%
    .banner-info
      display: flex
      position: absolute
      left: 0
      right: 0
      bottom: 0
      height: .6rem
      line-height: .6rem
      color: #fff
      background-image: linear-gradient(top, rgba(0,0,0,0), rgba(0,0,0,.8))  // 渐变色
      .banner-title
        flex: 1
        font-size: .4rem
        padding: 0 .4rem
      .banner-number
        margin-top: .14rem
        height: .32rem
        line-height: .32rem
        padding: 0 .4rem
        font-size: .24rem
        background: rgba(0, 0, 0, .8)
        border-radius: .1rem
        .banner-icon
          font-size: .24rem
</style>
